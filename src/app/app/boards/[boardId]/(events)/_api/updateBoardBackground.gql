mutation UpdateBoardBackground(
  $boardId: ID!
  $clientMutationId: ID!
  $background: BoardBackground!
) {
  updateBoardBackground(
    boardId: $boardId
    clientMutationId: $clientMutationId
    background: $background
  )
}
